{
  "tasks": {
    "start": "deno run --config deno.json -A main.ts",
    "dev": "deno run --watch main.ts",
    "test": "deno test -A --filter",
    "test:all": "deno test -A"
  },
  "importMap": "./import_map.json",
  "lock": false
}
